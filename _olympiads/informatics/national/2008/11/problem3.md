

### Задача C. Трубы

<table>
    <tr>
        <th>Ограничение по времени</th>
        <th>Ограничение по памяти</th>
    </tr>
    <tr>
        <td>2 секунды</td>
        <td>256 мегабайт</td>
    </tr>
</table>

Сколькими способами на площадке из $NxM$ ячеек можно расставить трубы 11-ти различных типов, приведенных на рисунке

#### Формат входного файла

Первая строка входного файла содержит три целых числа $N$, $М$ и $P$ (1 <= $N$ <= $8$, 1 <= $M$ <= $8$, 2 <= $P$ <= $10^9$). Числа разделены пробелом.


#### Формат выходного файла

Выходной файл должен содержать одно целое число — ответ на задачу.

#### Примеры

Вход
```
2 2 10
```

Выход
```
1
```
